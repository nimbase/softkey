# DEMO public verification keys for premium_cli. Safe to embed and ship.
#
# These are PUBLIC keys only: they verify signatures but cannot create
# them. They correspond to the fixed DEMO seeds in dev_keys.nim and
# exist so the example runs without setup. A production application
# embeds ITS OWN public keys here instead.
#
# Regenerate (after changing dev_keys.nim):
#   clue build examples/mint_license.nim --out:mint_license
#   ./mint_license  # prints "license pubkey hex" / "online pubkey hex"

const
  DemoLicenseKid* = "example-license-2026-01"
  DemoOnlineKid* = "example-online-2026-01"
  DemoIss* = "example-license-authority"
  DemoAud* = "nim-license-example"
  DemoOnlineAud* = "nim-license-example-online"

  DemoLicensePubkey*: array[32, byte] = [0xda, 0x29, 0xe9, 0x5b, 0x02,
    0xe0, 0x0f, 0xfa, 0x15, 0x64, 0x57, 0x75, 0xfb, 0x1d, 0x2b, 0xa2,
    0x22, 0xa1, 0x94, 0x33, 0x95, 0xee, 0xa0, 0x6b, 0x94, 0xe2, 0xc0,
    0x57, 0xb7, 0xbe, 0x69, 0xd0]

  DemoOnlinePubkey*: array[32, byte] = [0xa7, 0xfc, 0xf7, 0xdc, 0xed,
    0x55, 0x31, 0xd5, 0xac, 0x38, 0x5c, 0xc7, 0xbd, 0xa1, 0xa4, 0xeb,
    0x7d, 0x00, 0xd6, 0x24, 0x8a, 0x7f, 0x8f, 0xbd, 0x8d, 0xbb, 0xdd,
    0xf7, 0x3a, 0x21, 0xd2, 0xa0]
